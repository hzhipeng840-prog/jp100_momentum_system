# AGENTS.md

本ファイルは、このリポジトリで作業するAIコーディングエージェントおよび自動化ツール向けのプロジェクト規約です。リポジトリ直下とすべてのサブディレクトリに適用します。

## 1. プロジェクトの目的

本プロジェクトは、日本株の注目銘柄を抽出するための調査システムです。

- JPXの東証上場普通株一覧を基礎ユニバースとする。
- Yahoo!ファイナンスの日本株ランキングから当日の候補銘柄を抽出する。
- yfinanceの無料日足データから価格特徴量を計算する。
- 「注目Top100」と「毎日厳選3～5銘柄」を出力する。
- Streamlitのユーザー画面はすべて日本語とする。

本プロジェクトは情報整理と研究を目的とし、投資助言や自動売買は行いません。

## 2. 指示の優先順位

作業時は、以下の順序で制約を適用します。

1. 現在のタスクにおけるユーザーの明示的な指示。
2. 本 `AGENTS.md`。
3. 既存コード、テスト、ドキュメントで確立された規約。
4. 使用ツールの既定動作。

指示が競合する場合は、競合内容を説明し、無断で回避しません。

## 3. プロジェクト構成

```text
app.py                       Streamlit日本語画面。表示と操作のみを担当
daily_job.py                 日次更新のコマンドライン入口
cloud_job.py                 GitHub Actions向け営業日・重複判定付き入口
src/jp100/config.py          パス、データソース、実行設定
src/jp100/sources.py         JPX/Yahooデータの取得、解析、キャッシュ
src/jp100/market.py          yfinance株価データ取得と特徴量計算
src/jp100/scoring.py         Top100評価と毎日厳選
src/jp100/rules.py           因子・ルール定義と発動状態の固定
src/jp100/rule_evaluation.py 因子分位・IC・入替率・ルール比較
src/jp100/trading_calendar.py 東証営業日カレンダー
src/jp100/pipeline.py        データパイプラインの制御
src/jp100/history.py         候補・Top100・厳選の累積履歴
src/jp100/followups.py       翌日寄付・1/3/5/10営業日の成績追跡
src/jp100/freshness.py       価格取得率と結算状況の鮮度判定
src/jp100/backtest.py        厳選シグナルの簡易集計
src/jp100/portfolio.py       コスト込み仮想ポートフォリオ
src/jp100/universe.py        JPX流動性候補と評価版の比較
src/jp100/market_environment.py 市場環境・相関・集中リスク
src/jp100/watchlist.py       ローカルウォッチリスト
src/jp100/storage.py         原子的CSV/JSON出力とスナップショット公開
tests/                       実ネットワークに依存しない単体テスト
data/raw/                    外部原データのキャッシュ。Gitへ登録しない
data/processed/              生成結果。Gitへ登録しない
data/history/                日次累積履歴。Gitへ登録しない
data/snapshots/              実行単位の不変スナップショット。Gitへ登録しない
data/user/                   個人ウォッチリスト。Gitへ登録しない
scripts/verify.ps1           高速オフライン検証
scripts/export_cloud_data.py dataブランチ公開対象の抽出
scripts/sync_cloud_data.ps1  dataブランチからローカルへの同期
.github/workflows/           GitHub Actionsの日次自動更新
README.md                    利用者向け実行手順
AGENTS.md                    開発・エージェント作業規約
```

責務の境界：

- `app.py` に取得、評価、特徴量計算のロジックを重複実装しない。
- `pipeline.py` は処理順序を管理し、具体的な指標計算を実装しない。
- データソースの解析は `sources.py`、価格処理は `market.py` に置く。
- 評価ルールは `scoring.py` に集約し、重み変更時はテストと説明を更新する。
- ファイル出力は `storage.py` または明示的なキャッシュ関数を使用する。

## 4. 言語規約

- ユーザーに表示する画面、ボタン、表見出し、エラー、ダウンロード名は日本語とする。
- Pythonの識別子、モジュール名、ファイル名は英語を使用する。
- プロジェクト内の規約と利用者向けドキュメントは日本語を基本とする。
- コードコメントは簡潔な日本語とし、既存コードの表現に合わせる。
- CSVは `utf-8-sig`、JSONはUTF-8で保存し、Windows版Excelで日本語を読めるようにする。
- PowerShellスクリプトは、Windows PowerShell 5の文字コード問題を避けるため、原則としてASCII文字だけを使用する。

## 5. 作業モード

### 5.0 バージョン履歴

- 現在の正式版は `VERSION` の `vN` 形式で管理する。
- 利用者へ影響する機能、評価、データ構造、運用変更では新しい連番を作成する。
- 新版では `CHANGELOG.md` と `docs/versions/vN.md` を同じ変更内で更新する。
- 公開時は同じ番号のGitタグを付け、公開済みタグを付け替えない。
- 過去版の詳細文書を現在仕様へ書き換えず、訂正が必要な場合は新しい版で説明する。
- 詳細は `docs/versioning.md` に従う。

### 5.1 Planモードを先に使用する作業

以下の作業は、編集前に計画を提示します。クライアントがPlanモードに対応している場合は、Planモードを使用します。

- プロジェクト初期化またはディレクトリ構成の変更。
- 外部データソースの追加または置換。
- 候補ユニバース、評価式、評価重み、毎日厳選条件の変更。
- CSVまたはJSONの出力構造変更。
- 3つ以上のモジュールにまたがる機能追加。
- 新しい依存関係、データベース、クラウドサービス、定期処理、配備手順の導入。
- 2分を超える見込みのコマンド実行。
- データ削除、移動、上書き、Git履歴変更の可能性がある操作。

計画には最低限、以下を含めます。

1. 目的と完了条件。
2. 変更対象ファイル。
3. データソースとネットワークへの影響。
4. テスト段階と予想所要時間。
5. リスク、戻し方、禁止事項。

計画確認後に実装します。小規模な不具合修正、文言変更、単体テスト追加は直接実行できます。

### 5.2 実装中の規約

- 最初にコードと `git status` を確認し、作業ツリーが空だと決めつけない。
- 変更範囲を必要最小限に保ち、無関係なリファクタリングを行わない。
- 各段階の完了時に、最小コストの検証を実行する。
- ユーザーの既存変更を保持し、無断で元に戻さない。
- 問題発生時は原因を調査し、同じ失敗の自動再試行は1回までとする。
- 予想時間を超えた場合は、現在の段階と原因を直ちに報告し、無言で待ち続けない。

## 6. コマンドと実行時間

検証は以下の段階で行います。最初から実ネットワークを使う全量処理を実行しません。

### L1：静的確認と単体テスト

通常の変更で必ず実行します。目標時間は30秒以内です。

```powershell
powershell -ExecutionPolicy Bypass -File scripts/verify.ps1
```

同等のコマンド：

```powershell
python -m compileall -q app.py daily_job.py cloud_job.py src tests
python -m unittest discover -s tests -v
```

### L2：キャッシュ解析確認

`data/raw/` に保存済みのJPX/Yahooキャッシュだけを読み、ネットワークへ接続しません。データ解析処理を変更した場合だけ実行します。

### L3：単一銘柄のネットワーク確認

`7203.T` など、明示した1銘柄だけで確認します。目標時間は60秒以内です。

単一銘柄の確認が成功する前に、数百銘柄のyfinance全量取得を再実行してはいけません。

### L4：日次全量更新

以下の場合だけ実行します。

- ユーザーが最新データへの更新を明示的に求めた場合。
- データソースまたは価格取得処理を変更し、最終確認が必要な場合。
- 公開または納品前に最新結果を生成する場合。

```powershell
python daily_job.py --yahoo-pages 3 --daily-count 5
```

目標時間は3分以内です。3分を超えた場合は停止して原因を調査し、無制限に待機しません。

### L5：画面確認

```powershell
python -m streamlit run app.py
```

- `streamlit` コマンドが `PATH` に登録されていることを前提にしない。
- 自動確認では、非表示のバックグラウンドプロセスとして起動する。
- 起動前にポートを確認し、確認後は今回起動したプロセスを停止する。
- 作業終了時に、用途不明のPythonまたはStreamlitプロセスを残さない。
- サービス起動失敗の再試行は1回までとし、その後はエラーを報告する。

## 7. ネットワークと依存関係

- 純粋な業務ロジック変更ではネットワークへ接続しない。
- インストール前に対象モジュールが既に存在するか確認する。
- 新しい依存関係は `requirements.txt` に記録し、用途を説明する。
- 目的なく全依存関係を更新しない。
- ネットワーク障害時は既存キャッシュを優先し、外部サイトへ繰り返しアクセスしない。
- JPX/Yahooへのアクセスは低頻度とし、高並列取得やサイト制限の回避を行わない。
- yfinanceはバッチ取得を使用し、数百銘柄を1件ずつ高頻度で取得しない。
- Windowsの非ASCIIユーザーパスでは、CAファイルをプロジェクトの `.cache/` にコピーしてよい。
- TLS証明書検証を無効にしてはいけない。
- Cookie、APIキー、アカウント情報、個人パスをリポジトリへ保存しない。

## 8. データソースの境界

- JPXを、上場範囲、市場区分、銘柄名、業種分類の正式な情報源とする。
- Yahoo!ファイナンスのランキングは候補抽出だけに使用し、正式な終値情報源とはしない。
- yfinanceは無料の調査用価格データとして使用し、リアルタイム性、完全性、取引用途の精度を保証しない。
- 候補銘柄は必ずJPX普通株一覧と照合する。
- ETF、REIT、投資信託、対象外市場の商品を普通株Top100へ混入させない。
- 外部フィールド変更時は、日本語の明示的なエラーを出し、誤った結果を黙って生成しない。
- ネットワーク障害時にキャッシュを使用した場合、メタデータへ `cache_used` を記録する。

## 9. 評価と出力

- 評価方法は説明可能でなければならず、出所を説明できないブラックボックス得点を導入しない。
- ランキングでは最低限、モメンタム、トレンド、流動性、出来高、変動リスクを考慮する。
- 毎日厳選はTop100から選び、3～5銘柄に制限する。
- 毎日厳選では最低株価、平均売買代金、過熱度、業種分散を考慮する。
- 評価重みまたは選定条件を変更した場合は、テストも更新する。
- 最低限 `candidate_pool.csv`、`top100.csv`、`daily_picks.csv`、`metadata.json` を出力する。
- 生成失敗時に、直前の有効な結果を空ファイルで上書きしない。

## 10. テスト規約

- 単体テストは実ネットワーク、現在日時、外部サイトの稼働状況に依存させない。
- HTML、DataFrame、価格系列には最小限の固定サンプルを使用する。
- データ解析、MultiIndex価格分割、特徴量計算、ランキング、毎日厳選にテストを用意する。
- 不具合修正時は再現テストを先に追加するか、同じ変更内で追加する。
- 全量ネットワーク更新の成功を単体テストの代わりにしない。

## 11. 禁止操作

ユーザーが影響を理解した上で明示的に依頼しない限り、以下を禁止します。

- `git reset --hard`、`git clean -fd`、強制プッシュ、Git履歴の書き換え。
- ユーザーが作成した未コミット変更の削除または上書き。
- `data/`、`.git/`、ワークスペース全体の削除。
- 自動的なコミット、プッシュ、PR作成、リモートリポジトリ変更。
- システム全体の `PATH`、証明書設定、グローバルPython環境の変更。
- SSL/TLS証明書検証の無効化。
- テスト中の実注文、証券会社接続、自動売買処理。
- 本プロジェクトの結果を投資助言、利益保証、確実な予測として表現すること。
- ベンチマークやテストなしでの大規模リファクタリング。
- 1件の失敗コマンドに対する無制限な再試行または長時間の無言待機。

GitHub Actions自身が日次生成結果を専用の `data` ブランチへ公開する処理は、
レビュー済みの `.github/workflows/daily_pipeline.yml` に限り上記の例外とする。
コードを管理する `main` ブランチへ日次データを自動コミットしてはいけない。

## 12. 完了条件

タスク完了前に以下を確認します。

- 関連コードの構文確認が成功している。
- オフライン単体テストが成功している。
- ユーザー向け文言が日本語で統一されている。
- 秘密情報、個人のシステムパス、大容量キャッシュを追加していない。
- `git diff --check` に問題がない。
- 全量更新を実行した場合は、基準日、候補数、Top100件数、毎日厳選件数を報告する。
- サービスを起動した場合は、保持が必要か確認する。自動確認用サービスは必ず停止する。
- 実行していない検証を明記し、未確認事項を確認済みとして扱わない。
